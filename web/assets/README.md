Place the official OPPO logo asset at:

`web/assets/oppo-logo.svg`

The customer quotation template in `app.js` references this exact path for:

- in-system quotation preview
- HTML quotation export
- PDF quotation export
- print quotation rendering

Do not replace it with a redrawn or altered logo file.
